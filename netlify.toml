[build]
  # Publish directory contains the final static site. Use repository root so
  # sibling folders like /data and /public are published alongside /app.
  publish = "."
  # No build command required for this static site
  command = ""

[context.production.environment]
# Add production ENV overrides here if needed
